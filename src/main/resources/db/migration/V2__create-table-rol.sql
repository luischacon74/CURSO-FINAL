CREATE TABLE rol (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    rol VARCHAR(100) NOT NULL

);
INSERT INTO rol (rol) VALUES ('ADMINSITRADOR');
INSERT INTO rol (rol) VALUES ('PERFIL');
INSERT INTO rol (rol) VALUES ('VIEWER');